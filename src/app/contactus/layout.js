export const metadata = {
    title: "Contact Us | Biafo",
    description:
        "Contact page where user can send messages to Biafo",
};

export default function ContactUsLayout({ children }) {
    return <>{children}</>;
}
