export const metadata = {
    title: "About Biafo",
    description:
        "About Biafo page where user can learn more about Biafo and its vision",
};

export default function AboutUsLayout({ children }) {
    return <>{children}</>;
}
